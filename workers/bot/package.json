{
  "name": "ptt-notify-bot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 execute ptt-notify-bot-db --file=src/db/schema.sql"
  },
  "dependencies": {
    "grammy": "^1.34.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250327.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  }
}
