# HTML files — never cache so users always get the latest shell
/*.html
  Cache-Control: no-cache, no-store, must-revalidate

# Next.js hashed static assets — safe to cache long-term
/_next/static/*
  Cache-Control: public, max-age=31536000, immutable
